<script>
    // ... upar wala menu code waisa hi rahega ...

    function order() {
        let name = prompt("अपना नाम लिखें:");
        let time = prompt("किस समय/तारीख पर अपॉइंटमेंट चाहिए?");
        if(!name || !time) { alert("कृपया नाम और समय बताएं!"); return; }

        let total = document.getElementById('total').innerText;
        let msg = `*पंडित गोविंद शास्त्री जी को नई बुकिंग:%0A%0A👤 नाम: ${name}%0A📅 समय: ${time}%0A--------------------------%0A`;
        for (let i in cart) msg += `- ${i} (₹${cart[i]})%0A`;
        msg += `--------------------------%0A💰 कुल दक्षिणा: ₹${total}`;
        
        // Yahan Pandit ji ka WhatsApp number daalein
        window.open("https://wa.me/917014808515?text=" + msg);
    }
</script>
